/* import { createSelector } from "@reduxjs/toolkit";

const bookmarkSelector = (state) => state.bookmark.bookmarkLists;

export const bookmarkAvailable = createSelector([bookmarkSelector], (items) => {
    const isFound = items.find((item) => )
})  */