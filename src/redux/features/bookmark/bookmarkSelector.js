//import { createSelector } from "@reduxjs/toolkit";

//const bookmarkSelector = (state) => state.bookmark.bookmarkLists;

/* export const bookmarkTotal = createSelector([bookmarkSelector], (item) => {
    return item.reduce((total, current) => (total += current), 0)
}) */